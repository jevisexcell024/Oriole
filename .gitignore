# Dependencies
node_modules/

# Build output
dist/
dist-server/
*.tsbuildinfo
*.zip

# Word temp files
~$*

# Secrets & local env
.env
.env.*
!.env.example

# Embedded database (local dev) & migrated legacy data
server/.pgdata/
server/data.json
server/data.json.migrated

# Local scheduled DB backups (host-specific, not source)
backups/

# Logs & OS cruft
*.log
.DS_Store
